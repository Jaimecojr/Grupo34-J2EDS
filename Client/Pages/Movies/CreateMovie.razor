@page "/movies/create"
@inject IServiceMovie Smovie
@using peliculas.Client.Pages.Components
<div class="jumbotron">
    <h3>Formulario Crear Pelicula</h3>
    <FormCreateMovie OnValidSubmit = "crear" pelicreando = "pelicreandoC"></FormCreateMovie>
    @* <EditForm Model="pelicreando" OnSubmit="Create">
    <div class="form-group mb-3">
            <label class="form-label mb-3">Categoria</label>
            <div class="mb-3">
                <InputText class="form-control" @bind-Value="pelicreando.Titulo"></InputText>
            </div>
            <div class="mb-3">
                <button class="btn btn-success" type="submit">Crear</button>
            </div>
        </div>
    </EditForm> *@
</div>
@code{
    private Movie pelicreandoC = new Movie();
    private void Create(){

        Smovie.AddMovie(pelicreandoC);
        Console.WriteLine($"Nombre de la categoria de peliculas {pelicreandoC.Titulo}");
    }
    public void crear(){
        Console.WriteLine($"Pelicula: {pelicreandoC.Titulo}");
        Console.WriteLine($"Duracion: {pelicreandoC.Duracion}");
        Console.WriteLine($"Director: {pelicreandoC.Director}");
        Console.WriteLine($"Lenguaje: {pelicreandoC.Idioma}");
    }
    
    
}