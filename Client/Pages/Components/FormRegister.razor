<div class="container">
    <div class="jumbotron">
        <h2>Crear una cuenta</h2>
        <p>
            Registrarse para obtener una cuenta es gratis y fácil. Rellene el siguiente formulario para empezar.
        </p>
        <EditForm Model = "User" OnValidSubmit="OnValidSubmit">
            <DataAnnotationsValidator></DataAnnotationsValidator>
            <div class="form-group">
                <label>Correo</label>
                <InputText type="email" class="form-control"  @bind-Value="@User.EmailUser" placeholder="Ingresa email"></InputText>
                <ValidationMessage For="@(()=>User.EmailUser)"></ValidationMessage>
            </div>
            <div class="form-group">
                <label>Contraseña</label>
                <InputText type="password"  @bind-Value="@User.PasswordUser" class="form-control" placeholder="Contraseña"></InputText>
                <ValidationMessage For="@(()=>User.PasswordUser)"></ValidationMessage>
            </div>
            <button type="submit" class="btn btn-primary">Iniciar Sesion</button>
        </EditForm>
    </div>
</div>

@code {
    [Parameter] public User User {get;set;}
    [Parameter] public EventCallback OnValidSubmit {get;set;}
} 